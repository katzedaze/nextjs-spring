services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    # Do NOT expose the DB on the host by default.
    # For local DB introspection, copy docker-compose.override.yml.example and run
    # `docker compose -f docker-compose.yml -f docker-compose.override.yml up`.
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      APP_JWT_SECRET: ${JWT_SECRET}
      APP_JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS}
      APP_CORS_ALLOWED_ORIGINS: http://localhost:${FRONTEND_PORT}
    ports:
      - "${BACKEND_PORT}:8080"

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
      API_INTERNAL_BASE_URL: ${API_INTERNAL_BASE_URL}
      # Server-only — used by Next.js middleware to verify JWT signature on the edge.
      APP_JWT_SECRET: ${JWT_SECRET}
      # Opt out of Secure cookie only for local HTTP dev (see .env.example).
      COOKIE_INSECURE: ${COOKIE_INSECURE:-0}
    ports:
      - "${FRONTEND_PORT}:3000"

volumes:
  postgres-data:
